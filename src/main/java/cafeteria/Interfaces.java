package cafeteria;

// Legacy container removed. Concrete interfaces and implementations are defined
// in dedicated files or inside service/repository files to keep a single source
// of truth and avoid duplicate public type declarations.

